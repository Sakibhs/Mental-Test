import 'package:flutter/material.dart';

class Blog extends StatelessWidget{
  static String name = "Blog";
  const Blog({Key key}) : super(key:key);
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
      color: Colors.green,
    );
  }

}